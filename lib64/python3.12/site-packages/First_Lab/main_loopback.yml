---
- name: Creating loopback interfaces & advertising in BGP
  hosts: lab-r01
  connection: network_cli
  gather_facts: no

  vars_files:
    - vars/vars_l3_interfaces.yml

  tasks:
    - import_tasks: tasks/tasks_ios_loopback.yml

