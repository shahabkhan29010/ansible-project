- name: Convert spine interfaces to layer3
  cisco.nxos.nxos_interfaces:
    config:
      - name: "{{ item.interface }}"
        mode: layer3
        enabled: true
    state: merged
  loop: "{{ spine_interfaces }}"